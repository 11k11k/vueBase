import { Search, Swipe, SwipeItem, Grid, GridItem, Button, Tabbar, TabbarItem, NavBar, Toast } from 'vant'
import Vue from 'vue'

Vue.use(Button).use(Tabbar).use(TabbarItem).use(NavBar).use(Toast).use(Search).use(Swipe).use(SwipeItem).use(Grid).use(GridItem)
